To-do list:

- Allow parallelization with bootstrapping (but maybe make it mutually exclusive with spaxel parallelization...?)

- Implement an argument that allows the user to pass in a mask that manually determines the number of line components 
  that should be fit for each spaxel.
